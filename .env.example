# Copy to .env and fill in if you want live LLM analysis.
# Without a key, the agent runs in offline heuristic mode.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Optional: point at any OpenAI-compatible endpoint (Azure, Ollama, etc.)
# OPENAI_BASE_URL=http://localhost:11434/v1
